package compatibility_checker.factories;

import compatibility_checker.parts.Socket;

public class AMDSocket extends Socket {
    public AMDSocket(String socketType) {
        super(socketType);
    }
}
