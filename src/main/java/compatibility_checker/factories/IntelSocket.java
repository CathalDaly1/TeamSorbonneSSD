package compatibility_checker.factories;

import compatibility_checker.parts.Socket;

public class IntelSocket extends Socket {
    public IntelSocket(String socketType) {
        super(socketType);
    }
}
